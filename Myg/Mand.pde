class Mand{
 
  void mand(){
  ellipse(mouseX, mouseY-20, 20, 20);
  line(mouseX, mouseY, mouseX+20, mouseY+20);
  line(mouseX, mouseY, mouseX-20, mouseY-10);
  line(mouseX, mouseY, mouseX, mouseY+30);
  line(mouseX, mouseY+30, mouseX+20, mouseY-30);
  line(mouseX, mouseY, mouseX, mouseY);
  }
  
  
  
  
}
